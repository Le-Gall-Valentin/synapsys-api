# ============================================================
# SYNAPSYS API — Environment Configuration
# Copy this file to .env and fill in the values.
# Never commit your actual .env file.
# ============================================================

# ── PostgreSQL ──────────────────────────────────────────────
SYNAPSYS_DB_URL=jdbc:postgresql://localhost:5432/synapsys
SYNAPSYS_DB_USER=synapsys
SYNAPSYS_DB_PASSWORD=synapsys

# ── JWT Access Token ─────────────────────────────────────────
# Secret key for signing access tokens.
# IMPORTANT: must be a cryptographically random value of at least 256 bits (32 bytes).
# Generate with: openssl rand -base64 32
# WARNING: a predictable value (e.g. words, repeated chars) weakens token security.
SYNAPSYS_JWT_SECRET=<generate with: openssl rand -base64 32>
# Access token lifetime in minutes (default: 15).
SYNAPSYS_JWT_EXPIRY_MINUTES=15

# ── Refresh Token ────────────────────────────────────────────
# Opaque refresh token lifetime in days (default: 30).
SYNAPSYS_REFRESH_TOKEN_EXPIRY_DAYS=30

# ── Cookies ──────────────────────────────────────────────────
# Set to false for local HTTP development; defaults to true in production (HTTPS).
SYNAPSYS_COOKIE_SECURE=false

# ── CORS ─────────────────────────────────────────────────────
# Comma-separated list of allowed origins for the frontend.
SYNAPSYS_CORS_ALLOWED_ORIGINS=http://localhost:5173

# ── Database Seeder ──────────────────────────────────────────
# Initial admin account created on first run.
SYNAPSYS_SEED_USERNAME=admin
SYNAPSYS_SEED_EMAIL=admin@synapsys.local
SYNAPSYS_SEED_PASSWORD=changeme

# ── Redis ────────────────────────────────────────────────────
# Required at startup — the application will not start if Redis is unavailable.
# Format: redis://[password@]host:port[/db-index]
#         redis://localhost:6379        (no auth)
#         redis://:password@host:6379   (with password)
#         rediss://host:6380            (TLS)
SYNAPSYS_REDIS_URL=redis://localhost:6379

# ── Swagger UI ───────────────────────────────────────────────
# Disabled by default in production; enable locally for API exploration.
SWAGGER_ENABLED=true

# ── Encryption ─────────────────────────────────────────
# IMPORTANT: must be a cryptographically random value of at least 256 bits (32 bytes).
# Generate with: openssl rand -base64 32
SYNAPSYS_ENCRYPTION_SECRET=<generate with: openssl rand -base64 32>

# ── Agents ───────────────────────────────────────────────
# Enrollment token validity in hours (default 24).
SYNAPSYS_AGENT_TOKEN_VALIDITY_HOURS=24
# WebSocket challenge nonce TTL in seconds (default 30).
SYNAPSYS_AGENT_CHALLENGE_TTL_SECONDS=30
# Agent presence TTL in seconds - agent is "down" past this without heartbeat (default 90).
SYNAPSYS_AGENT_PRESENCE_TTL_SECONDS=90
# Enrollment token prefix (visual identifier), default synenr_.
SYNAPSYS_AGENT_TOKEN_PREFIX=synenr_
# Agent WebSocket path, default /ws/agents.
SYNAPSYS_AGENT_WEBSOCKET_PATH=/ws/agents

