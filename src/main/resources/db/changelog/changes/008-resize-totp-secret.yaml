databaseChangeLog:
  - changeSet:
      id: 008-resize-totp-secret
      author: system
      changes:
        - modifyDataType:
            tableName: users
            columnName: totp_secret
            newDataType: VARCHAR(256)